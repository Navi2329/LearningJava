//variables and data types
public class variables {
    public static void main(String[] args) {
        int i=3;
        double d=3.3;
        float f=4;
        char c='c';
        System.out.println(c+d); //typecasts char to int using ascii
        System.out.print(i+f);

    }
}
/*
1 bit=bit
4 bits=nibble
8 bits=byte
16 bits=word
 */

/*
PRIMITIVE DATA TYPES
boolean-1 bit(default=false)
char=2 byte(default='\u0000')
byte=1 byte ,short=2 bytes,int 4 bytes(default=0),long=8 bytes(default=0L)
float=4 bytes(default=0.0f) ,double=8 bytes(default=0.0d)
 */

/*
NON-PRIMITIVE DATA TYPES
STRINGS,ARRAYS,OBJECTS,ETC
 */